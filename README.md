# 🏢 회사 용어 위키 (Company Wiki)

팀 내 용어 통일을 위한 **데스크톱 용어 사전** 애플리케이션

## ✨ 주요 기능

- 📚 **용어 관리**: 용어명, 정의, 예시, 동의어 등록 및 관리
- 🔍 **실시간 검색**: 용어명, 정의, 동의어 통합 검색
- 📁 **카테고리**: 부서별/도메인별 분류 (색상 지정 가능)
- ⚡ **즉시 반영**: 승인 절차 없이 바로 수정 적용
- 📜 **변경 이력**: 모든 편집 내역 자동 기록
- 👥 **권한 관리**: 관리자/일반 사용자 구분

## 🛠️ 기술 스택

- **UI**: Tkinter
- **Database**: SQLite
- **Language**: Python 3.x

## 📦 설치 및 실행

```bash
# 저장소 클론
git clone https://github.com/YOUR_USERNAME/company-wiki.git
cd company-wiki

# 실행 (별도 패키지 설치 불필요)
python main.py
```

> 💡 Python 3.x만 설치되어 있으면 바로 실행 가능합니다.

## 📁 프로젝트 구조

```
company_wiki/
├── main.py              # 앱 진입점 & 로그인
├── database.py          # SQLite 연결 & 스키마
├── models.py            # 데이터 클래스
├── repository.py        # 데이터 액세스 레이어
├── ui/
│   ├── __init__.py
│   ├── styles.py        # 색상, 폰트, 스타일
│   ├── main_window.py   # 메인 윈도우 레이아웃
│   ├── term_list_view.py      # 용어 목록
│   ├── term_detail_dialog.py  # 용어 편집
│   ├── category_view.py       # 카테고리 관리
│   └── history_view.py        # 변경 이력
└── wiki.db              # SQLite DB (자동 생성)
```

## 📸 스크린샷

(앱 실행 후 스크린샷 추가 예정)

## 🚀 사용 방법

1. **로그인**: 사용자 이름 입력 (첫 입력 시 자동 계정 생성)
2. **용어 추가**: `➕ 새 용어` 버튼 클릭
3. **용어 검색**: 검색창에 용어 입력 또는 카테고리 필터 사용
4. **편집**: 용어 더블클릭 또는 `✏️ 편집` 버튼
5. **히스토리**: 사이드바 `📜 히스토리` 메뉴에서 변경 이력 확인

## 🔧 기존 앱에 통합하기

```python
from company_wiki.ui.term_list_view import TermListView
from company_wiki.database import init_database
from company_wiki.repository import UserRepository

# 데이터베이스 초기화
init_database()

# 사용자 가져오기
user = UserRepository.get_or_create("사용자명")

# 위젯으로 사용
term_view = TermListView(parent_frame, user)
term_view.pack(fill='both', expand=True)
```

## 📄 라이선스

MIT License
